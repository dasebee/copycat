{
  "name": "phantom",
  "version": "1.0.0",
  "description": "html5 up phantom  template copy",
  "main": "index.html",
  "scripts": {
    "pug": "pug -w assets/index.pug -o ./",
    "css": "node-sass --output-style compressed assets/scss/style.scss -o ./",
    "watch": "npm run css && npm run css -- -w"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "node-sass": "^4.6.0",
    "pug": "^2.0.0-rc.4"
  }
}
